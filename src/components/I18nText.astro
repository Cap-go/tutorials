---
import { t } from '@/services/messages'

interface Props {
  k: string
  class?: string
  tag?: keyof HTMLElementTagNameMap
}

const { k, class: className, tag: Tag = 'span' } = Astro.props
---

<Tag data-i18n={k} class={className} set:html={t(k)} />

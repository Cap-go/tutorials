```markdown
---
"title": "Using capacitor-mock-location-checker Package Tutorial",
"description": "A tutorial on how to use the capacitor-mock-location-checker package in your Capacitor project.",
"created_at": "2022-02-01",
"published": true,
"slug": "capacitor-mock-location-checker.git"
---

# Using capacitor-mock-location-checker Package Tutorial

Here is a tutorial on how to use the capacitor-mock-location-checker package in your Capacitor project.

## Install

```bash
npm install capacitor-plugin-mocklocation
npx cap sync
```

## API

### echo(...)

```typescript
echo(options: { value: string; }) => Promise<{ value: string; }>
```

| Param         | Type                            |
| ------------- | ------------------------------- |
| **`options`** | <code>{ value: string; }</code> |

**Returns:** <code>Promise&lt;{ value: string; }&gt;</code>

---

### check(...)

```typescript
check(options: { value: string; }) => Promise<{ value: string; }>
```

| Param         | Type                            |
| ------------- | ------------------------------- |
| **`options`** | <code>{ value: string; }</code> |

**Returns:** <code>Promise&lt;{ value: string; }&gt;</code>

---

This tutorial provides an overview of the capacitor-mock-location-checker package API and how to use it in your project.
```
```
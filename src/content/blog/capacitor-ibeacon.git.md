I don't know the exact details of how to use the `@capgo/capacitor-ibeacon` package, but I can guide you on how to create a markdown tutorial following your requirements. You can follow the steps below:

1. Create a new markdown file with the title of the blog and the content for the tutorial.

```markdown
---
"title": "How to use @capgo/capacitor-ibeacon Package"
"description": "A tutorial on integrating the @capgo/capacitor-ibeacon package with your Capacitor app."
"created_at": "2022-01-10"
"published": true
"slug": "capacitor-ibeacon.git"
---

# Using @capgo/capacitor-ibeacon Package

In this tutorial, we will walk you through the steps to integrate the `@capgo/capacitor-ibeacon` package with your Capacitor app.

## Step 1: Install the Package

First, you need to install the `@capgo/capacitor-ibeacon` package by running the following command:

```bash
npm install @capacitor/ibeacon
```

## Step 2: Configuration

Next, you'll need to configure the package in your Capacitor project. Make sure to follow the specific setup instructions provided by the package documentation.

## Step 3: Implement iBeacon Features

Once the package is installed and configured, you can start implementing the iBeacon features in your app. This could include detecting nearby iBeacons, ranging beacons, and triggering actions based on beacon proximity.

## Step 4: Testing

Finally, test the implementation thoroughly to ensure that the iBeacon functionality works as expected in your app.

That's it! You've successfully integrated the `@capgo/capacitor-ibeacon` package into your Capacitor app.
```

Please replace the content inside the tutorial with the actual steps and details for integrating the `@capgo/capacitor-ibeacon` package with your Capacitor app.